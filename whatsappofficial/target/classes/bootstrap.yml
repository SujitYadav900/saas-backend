spring:
  application:
    name: whatsappofficial-service

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
logging:
  file:
    name: /tmp/wabuissness.log
